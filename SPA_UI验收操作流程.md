# SPA 真实人工 UI 验收 · 操作流程

> 状态：V1.3 代码冻结 · 人工验收阶段（2026-08-09 用户拍板）
> 验收通过 → V1.4 收尾（P0 错误定位 / P0 Dirty 状态 / P0 退出保护 / P1 WS 状态视觉）
> 本流程不碰任何 JSON，全部走正式 UI。

---

## 0. 前置准备（约 2 分钟）

1. 确认 **ComfyUI Desktop 已启动**，端口 8188，`MiniMaxH3Director` 节点正常加载。
   - 浏览器开 `http://127.0.0.1:8188`，节点列表能搜到 `MiniMaxH3Director` 即 OK。
2. 启动 SPA：
   ```
   cd D:\夸克网盘\ComfyUI_MiniMaxH3_Director\frontend
   npm run dev
   ```
3. 浏览器打开 `http://127.0.0.1:5173`。

> **注意 ①**：顶栏「已连接/未连接」圆点只在首次连接时变绿；中途 WS 断线圆点**不会实时变灰**（这是已知缺口，V1.4 的 P1 才补）。验收时不用管圆点，任务进度靠 3s 轮询兜底照常走。

## 1. 进入项目

1. 首页（项目选择页）往下滚，找到底部 **开发工具** 折叠区。
2. 点「**载入示例工程**」。
   - 示例项目《赛博都市》：**2 个场景 / 3 个镜头**
     - 场景 1「霓虹街区」：镜头 1「雨夜登场」（s1）、镜头 2「回眸」（s2）
     - 场景 2「数据塔」：镜头 3「抵达数据塔」（s3）
   - 你之前说的「Shot 03」= **第 3 个镜头「抵达数据塔」**（在场景 2 数据塔下面）。
3. 确认进入工作台：左侧分镜树、中间播放器、右侧镜头设置、底部任务中心都出现。

---

## 2. 步骤①：正常单镜生成（核心链路）

**操作**：
1. 左侧点镜头 **「抵达数据塔」（Shot 03）**。
2. 右侧「画面」Prompt 框，改一句，例如：
   `林雪走向高耸的数据塔入口，全息广告在玻璃幕墙上闪烁，镜头从身后仰拍跟随，雨滴打在全息屏上溅起光晕。`
3. 展开「**📑 更多分区**」，改一句「摄影」或「风格」，确认下方「**生成用 Prompt**」预览**实时同步变化**。
4. 顶栏生成范围下拉选「**当前镜头**」，点「**▶ 生成 当前镜头（1）**」。

**观察**：
- 任务中心（底部）出现 ⚡ 运行中任务，进度条推进、显示「镜头 1/1」+ 阶段。
- 左侧 Shot 03 状态灯变蓝（生成中）。
- 完成后任务变绿，状态灯变绿。

**PASS 判定**：
- [ ] 改 Prompt → 生成用 Prompt 同步
- [ ] 任务中心及时显示且推进
- [ ] H3 生成完成后**播放器自动切换到新视频**并播放（约 3-6 分钟，取决于时长）

> **注意 ②**：生成完成前播放器可能显示 Shot 03 的预览帧（全局预览槽，非镜头级），这是设计不是 bug。

## 3. 步骤②：生成中切换 Shot

**操作**：在 Shot 03 生成进行中（任务还在转）：
1. 点左侧 **「回眸」（Shot 02）**。
2. 改它的 Prompt 一句。
3. 看右侧面板、左侧状态灯、任务中心是否仍各自独立。
4. 再点回 **「抵达数据塔」（Shot 03）**。

**PASS 判定**：
- [ ] 右侧面板正确显示各自 Prompt（不串）
- [ ] 任务中心仍跟踪 Shot 03 的任务（不被切换打断）
- [ ] Shot 03 生成完成后正常出片、播放器正常更新
- [ ] 生成中的 Shot 03 没被你在 Shot 02 上做的修改影响

## 4. 步骤③：生成中编辑其他 Shot（快照隔离）

**操作**：再发起一次 Shot 03 生成；进行中：
1. 编辑 **「回眸」（Shot 02）** 的 Prompt（改动大一点，比如整句换掉）。
2. 切换到 **场景 1「霓虹街区」**，继续编辑其它镜头。
3. 等 Shot 03 生成完成。

**PASS 判定**：
- [ ] 正在跑的任务用的是**提交瞬间的参数**——你在生成中改的东西没有污染它（对比生成出的片是不是按你提交时的 Prompt）
- [ ] 生成完成后，新改的 Prompt 只影响**下次**生成

> 架构上已经保证（`buildTimelineStructure` 在提交瞬间快照），这一步是真人确认。

## 5. 步骤④：参考图

**操作**：
1. 选一个镜头，右侧「参考素材」→「🖼 参考图」。
2. 上传一张图（可先用现有 `linxue.png`，或换一张特征明显的图）。
3. 确认缩略图出现。
4. 删除它，再重新上传一张。
5. 发起该镜头生成，验证参考图确实传给 H3。

**PASS 判定**：
- [ ] 上传 → 缩略图正常
- [ ] 删除 → 列表清空
- [ ] 重新上传 → 新图替换生效
- [ ] 生成时确实传给 H3 —— 二选一验证：
  - **A. 看成片贴合**：参考图用特征明显的形象（如特定颜色服装），成片里人物贴合 → refs 生效
  - **B. 看节点数据**：ComfyUI 打开执行过的 MiniMaxH3Director 节点，检查 `timeline_data` 里对应段的 `refs[].imageFile` 是否填了路径

> **注意 ③**：如果缩略图是 emoji 占位而非图片，说明 `input/minimax_studio/...` 缺文件，需先上传一份再测。

## 6. 步骤⑤：切换回来（状态保持）

**操作**（Shot 03 已生成成功为前提）：
1. 确认 Shot 03 状态灯**绿**。
2. 切 **「回眸」（Shot 02）**。
3. 切 **场景 1「霓虹街区」**。
4. 再切回 **「抵达数据塔」（Shot 03）**。

**PASS 判定**：
- [ ] 状态灯仍绿（成功状态保持）
- [ ] Prompt 内容没丢（画面/分区字段都在）
- [ ] refs 没丢（参考图还在）
- [ ] 播放器正常

> **注意 ④**：播放器成片槽是**全局单槽**。如果中途你用「▶ 播放此镜」播过 Shot 02，播放器会显示 Shot 02 的片——因为最后播谁就留谁。**绿灯/Prompt/refs 是镜头级的不会丢**，只有「播放器里留哪段」是全局的。如果你期望「回到某镜头就自动显示它的片」，这算一个真实体验缺口，值得记进 V1.4 打磨。

---

## 验收结果记录

> 状态：**V1.3 人工验收基本通过（2026-08-10 用户确认）**——5 步全过，个别细节疑惑用户
> 表示等以后再遇到时再解决（见下方「待观察」）。V1.3 视为**可以封版**，进入 V1.4 收尾。

| 步骤 | 结果（PASS/FAIL） | 备注 |
|---|---|---|
| ① 正常单镜生成 | PASS | 生成闭环、任务中心、状态灯正常 |
| ② 生成中切换 Shot | PASS | 面板/任务各自独立不串 |
| ③ 生成中编辑其他 Shot | PASS | 快照隔离生效，提交瞬间参数不被污染 |
| ④ 参考图 | PASS | #85 修复后 @ 引用参考图可注入；建议后续用多张图再实测一遍贴合度 |
| ⑤ 切换回来 | PASS | 绿灯/Prompt/refs 状态保持 |

### 待观察（用户暂缓，等再遇到时单独拎出）

- 验收过程中用户提到「有的有疑惑」，未展开。此处留位，等用户再次遇到时记录具体现象
  与期望，再单独修复。

全部 PASS → V1.3 正式封版 → V1.4 收尾。
任何 FAIL → 单独拎出，说明「点哪一步、看到什么、期望什么」。
