# V1.5 素材库 / @资产 / 继承可视化 —— 规划文档

> 状态：**✅ CLOSED / FROZEN（2026-08-10）**。
> P0-A（素材库）+ P0-B（@资产改名同步）+ P1（四类继承可视化）三阶段全部收官，
> 前端 228 测试 + build 全绿，双目录已同步，纯前端刷新即生效（CHANGELOG #94/#95/#96）。
> **V1.5 正式冻结，不再追加功能。** 下一阶段 = 真实短片项目完整走查 V1.4+V1.5 合体验证
> 生产效率与资产一致性（不先开 V1.6 开发）。
>
> **冻结范围（保持不动）**：
> - ❌ H3 `integrated_multimodal_description` 高级结构化输入
> - ❌ AI 自动拆 Prompt / 自动 Prompt 优化
> - ❌ AI 自动生成 / 自动理解资产
> - ❌ 后端 `@prop/@style` 扩展
> - ❌ 资产 ID 绑定 Prompt（`@` 保持纯文本）
> - ❌ 全局资产与场景资产实时同步（复制是一次性快照，解耦）
>
> 版本边界：
> ```
> V1.2 分镜工作台 → V1.3 Prompt 分区 → V1.4 稳定生产闭环 → V1.5 素材库/@资产/继承
> 项目 ├─ 场景 ├─ 素材 └─ Shot └─ Prompt/@资产/继承/refs/H3生成 + 全局素材库
> ```
>
> 核心决策由用户拍板（见 §8），本文档是实施依据。先文档后代码。
> 关联：`CHANGELOG.md`（历史）、`frontend/src/models/project.ts`（数据模型）、
> `director/gen_timeline.py` + `director/plan.py`（后端资产解析）。

---

## 1. 一句话目标与边界

**让角色/地点/道具/风格资产真正成为「项目级可复用对象」**，把这条数据链彻底打通：

```
素材库 → 创建资产 → 上传参考图 → 场景设置默认 → Shot 显式/@引用
       → 解析最终生效资产 → 继承可视化 → refs 注入 timeline_data → H3 生成
```

**V1.5 明确不做**（下一层智能化，不碰）：
- H3 高级 Prompt 结构化输入（`integrated_multimodal_description` 等）
- 自动 Prompt 优化 / AI 自动拆 Prompt
- 自动场景理解 / 自动资产生成

---

## 2. 现状盘点（2026-08-10 代码调研）

### 2.1 已经实现（V1.5 无需重造）

| 能力 | 位置 |
|---|---|
| 资产模型四类 `Asset{id,name,kind,imageFile,aliases,description}` | `models/project.ts` |
| 两级资产池（全局 `episode.assets` + 场景 `scene.assets`） | `models/project.ts` + `stores/workbench.ts:assetPool` |
| 资产增删改 + **删除自动清理引用**（scene 默认 + shot.castId/locationId） | `stores/workbench.ts:addAsset/updateAsset/removeAsset/cleanupAssetRefs` |
| 参考图真实上传 ComfyUI `input` 目录 | `ImageUploadBox.vue` + `/upload` |
| `@资产` 文本解析（前端子串匹配） | `core/assetResolver.ts:resolveMentions` |
| `@资产` 后端兜底匹配（命名匹配） | `director/gen_timeline.py:_match_asset_by_name` |
| 三级继承链 **显式 > @匹配 > 场景默认 > 项目默认**（前后端一致） | `core/inheritanceResolver.ts` + `gen_timeline.py` 段解析 |
| 继承徽标「生效：xxx + ⚡镜头指定/↑场景默认/↑项目默认」 | `WorkbenchView.vue:inherit-line/inherit-badge` |
| 资产图注入 refs（cast/location/场景 props+styles/`<picture>`标签） | `gen_timeline.py` + `plan.py:SegmentPlan.cast_asset/location_asset/extra_assets/tag_assets` + `executor_core.py` |
| `@` 补全候选（全局资产 + 场景资产 + 场景参考图 + 段 refs，来源 🎬/🌐 标记） | `core/promptMediaTags.ts:buildMediaTagCandidates` |

### 2.2 V1.5 真实差距

| # | 差距 | 说明 |
|---|---|---|
| G1 | **素材库管理 UI 分散** | 资产在场景面板里逐个加，没有统一「素材库」入口（全局/场景切换、四类网格、描述、复制）。 |
| G2 | **重命名资产后 `@旧名` 失效** | `@` 是纯文本匹配（无 ID 绑定）。改名后 Prompt 里旧名不再命中，用户察觉不到，生成悄悄丢失锚点。 |
| G3 | **继承可视化只覆盖人物+地点** | `resolveInheritance` 只解析 cast/location；道具/风格作为场景素材组整组静默注入，用户看不到「最终生效了哪些道具/风格」。 |
| G4 | **两层池无联动** | 全局库与场景副本是独立条目、独立 id。从全局复制到场景目前无快捷操作，靠手动重建。 |

---

## 3. 数据模型设计（核心）

### 3.1 Asset 条目（沿用现有，零改动）

```ts
interface Asset {
  id: string;            // crypto.randomUUID，全局唯一、稳定、不可变
  name: string;          // 显示名 + @匹配名（同池内同 kind 唯一，见 3.5）
  kind: "cast" | "location" | "prop" | "style";
  imageFile: string;     // ComfyUI input 相对路径（/view 预览）
  description?: string;  // 备注（V1.5 新增 UI 录入，字段已存在）
  aliases?: string[];    // 额外 @匹配名
}
```

### 3.2 两级资产池（保持现有结构，补「复制」语义）

- **全局池** `episode.assets: SceneAssets` —— 项目级可复用资产。
- **场景池** `scene.assets: SceneAssets` —— 场景素材组（独立副本，独立 id）。
- **复制语义**（新）：场景面板「从全局复制」→ 以全局条目为模板在场景池 `addAsset`（生成新 id）。
  复制是**一次性快照**，复制后再改全局不影响场景副本（两级池解耦，风险最低）。
- **同池内同 kind 名称唯一**（新约束）：防止 `@名字` 匹配歧义。`addAsset`/`updateAsset`
  校验冲突时提示换名（不改历史数据，仅新操作校验）。

### 3.3 引用关系（三种，各有归宿）

| 引用形式 | 载体 | 解析层 | 持久化 |
|---|---|---|---|
| **@文本引用** | Prompt 里的 `@林雪` | 前端 `resolveMentions` / 后端 `_match_asset_by_name`（子串匹配 name/alias） | Prompt 纯文本本身 |
| **镜头显式** | `shot.castId/locationId` + `castManual/locationManual` | 继承链最高优先 | Shot 字段 |
| **场景默认** | `scene.defaultCastId/defaultLocationId/defaultStyleId` | 继承链兜底（显式/@ 未命中时） | Scene 字段 |

> 结论：**不引入「资产 ID 绑定」进 Prompt**。`@` 保持纯文本（用户拍板，§8.1），
> 引用关系靠「重命名时批量替换文本」（G2 修法，见 3.5）与「继承链解析」共同闭环。

### 3.4 继承链最终解析（P1 扩展到四类）

**单选类（cast / location）** —— 沿用现有链：
```
最终生效 = 镜头显式(id) > @匹配(同 kind) > 场景默认(defaultCastId) > 项目默认(全局池第一个)
```

**集合类（prop / style）** —— 新增「集合级生效」展示（不动后端注入逻辑）：
```
最终生效集合 = 场景池 props/styles（优先） ∪ 全局池 props/styles 中「场景池没有的同名资产」（补充）
来源标记：资产来自场景池 → ↑场景素材；来自全局池 → ↑项目默认
```
> **去重语义（用户 2026-08-10 补，必守）**：场景池条目可能是从全局复制出来的独立副本（新 id），
> 直接 `∪` 会让同一资产出现两份（林雪/林雪）。合并规则 = **按名称判重，场景池优先**，
> 全局池只补充场景池没有该名字的资产。UI 永不出现重复条目。
> 说明：prop/style 不引入「镜头单选 propId + 场景默认 defaultPropId」——那是 V2 场景理解的
> 范畴。V1.5 只在**展示层**把「该镜最终注入的 props/styles 集合 + 每条来源」画出来，
> 注入逻辑（extra_assets 整组）保持现状。

**四类统一展示**（`resolveInheritance` 扩展）：
```
InheritedAsset {
  assetId, asset,
  source: "shot" | "scene" | "project",
  matchedBy: "explicit" | "mention" | "scene-default" | "project-default"
}
```

### 3.5 @文本引用的改名替换（G2 修法）

- **触发**：`updateAsset` 改 name / aliases 时。
- **动作**：扫描项目全部镜头 Prompt（`content.visual` + 分区文本 `cameraText/style/soundText`），
  将 `@旧名` 替换为 `@新名`。
- **token 边界规则（用户 2026-08-10 补，必守）**：
  1. `@旧名` **后一个字符不是资产名字符**（`[\p{L}\p{N}_\-（）()·.·]`）时才替换；
  2. **优先匹配最长名称**：替换顺序按名称长度降序（`@林雪·成年` → `@林雪` → `@林`），
     长名先替换后不会残留可被短名二次命中的片段；
  3. 旧 alias 同理替换（同一套边界规则）。
- **实现位置**：`stores/workbench.ts` 新增 `renameAsset(kind, assetId, patch)` ——
  调 `updateAsset` + 遍历 `episode.scenes[].shots[].content` 做边界替换，全程 `touch()`。
- **不做**：不解析 `<picture>旧名</picture>` 标签（标签内名字是资产匹配名，改名后
  标签内名字与资产名不一致会失配 —— 一并替换，规则同上）。

### 3.6 引用完整性（沿用现有，补展示）

- 删除资产：`cleanupAssetRefs` 已清理 scene 默认 + shot 显式引用（不动 Prompt 文本，
  残留 `@旧名` 自然失配 —— 可接受，删除是明确操作）。
- 复制资产：新 id，无引用迁移。
- 改名资产：见 3.5，全量替换 `@` 文本引用，显式 id 引用不受影响（id 稳定）。

---

## 4. 闭环链路（最终形态）

```
素材库（场景面板增强）
  ↓ 创建「林雪」+ 上传角色参考图
全局池 cast[林雪]
  ↓ 「从全局复制」到场景 / 或直接场景新建
场景「霓虹街区」素材组 cast[林雪] + defaultCastId=林雪
  ↓ Shot 03
Prompt: @林雪走进@霓虹街区
  ↓ 前端 resolveMentions（@ 命中）→ mentionSet
  ↓ resolveInheritance（扩展四类）→ 最终生效资产 + 来源链
  ↓ 继承可视化：Shot 卡片「生效：林雪(⚡镜头指定) / 霓虹街区(↑场景默认)」+ 道具/风格集合
  ↓ 提交生成（Adapter → timeline_data）
  ↓ 后端 gen_timeline 段解析：cast_asset/location_asset/extra_assets/tag_assets
  ↓ refs 注入（资产图 → ref_image 槽位）
  ↓ executor → MiniMax H3 → 生成视频
```

---

## 5. 分期实施

> **实施节奏（用户 2026-08-10 拍板）**：P0-A → P0-B → P1，**每阶段独立验收、不混做**。
> 项目已较复杂，分开做便于定位回归。每阶段收尾验收通过后再进下一阶段。

### V1.5-P0-A：素材库 UI + 两级池 + 复制（G1 + G4）

目标：让「项目级资产库」有统一管理界面，两层池可复制。

- [x] 场景面板内「素材库」区升级（#94）：
  - 四类资产（人物/地点/道具/风格）tab 或分组网格：缩略图 / 名称 / 描述 / kind 标签。
  - 全局池 / 当前场景池 两级切换（来源标记 🎬 场景 / 🌐 全局 已有，复用）。
  - 资产卡片操作：新建（含名称+描述+上传参考图）、编辑（改名/描述/换图/加 alias）、删除、复制到另一池。
  - 复制快捷：「从全局复制到本场景」（G4）。
- [x] 同池内同 kind 名称唯一校验（3.2）。
- [x] 纯前端为主；`models/project.ts` 零改动（字段已够），store 补 `copyAssetToScene`（P0-A 不含 renameAsset，后者归 P0-B）。
- [x] **验收门**：新建项目建 4 类资产、上传图、从全局复制到场景、重开项目全部保留；
  场景池与全局池同名资产在 UI 中不显示重复。**✅ 已验收（#94，218 测试+build 全绿）**

### V1.5-P0-B：@资产改名同步（G2）

目标：`@资产` 引用链路完整，改名/删除不产生静默失联。

- [x] `renameAsset` 改名时按 §3.5 token 边界批量替换 `@旧名 → @新名`（#95）。
- [x] 替换覆盖：cast/location/prop/style 四类 + aliases + 四分区文本。
- [x] 单元测试：改名替换边界（`@林` 不误伤 `@林雪`、最长优先 `@林雪·成年`、alias 替换、四分区覆盖、多镜头扫描）。
- [x] **验收门**：改名「林雪」→「林雪·成年」后，所有镜头 Prompt 的 `@林雪` 变为 `@林雪·成年`，
  且 `@` 补全/高亮正常命中新名；同时存在 `@林`/`@林雪`/`@林雪·成年` 时无误替换。
  **✅ 已验收（#95，224 测试+build 全绿）**

### V1.5-P1：四类继承可视化（G3）

目标：用户一眼看到这个 Shot 最终用了谁、从哪层来。

- [x] `resolveInheritance` 扩展（#96）：
  - cast/location：沿用单选链（显式 > @ > 场景默认 > 项目默认）。
  - prop/style：新增「集合级」解析（**场景池同名优先，全局池补缺**，见 §3.4）。
- [x] Shot 卡片「生效资产」区：四类分块展示，每条资产 = 缩略图 + 名 + 来源徽标
  （⚡镜头指定 / ↑场景默认 / ↑项目默认 / 🎬场景素材 / 🌐全局素材）。
- [x] ~~后端 `@prop/@style` 匹配~~ —— **第一轮 V1.5 不做**（用户 2026-08-10 拍板，见 §8.7）。
- [x] **验收门**：镜头依赖场景默认 + 显式 + @ 命中混合时，卡片准确展示每条生效资产及来源；
  道具/风格集合与后端实际注入一致（无重复条目）；真机生成闭环后 V1.5 冻结。
  **✅ 已验收（#96，228 测试+build 全绿）** —— 真机生成闭环走查排在下一阶段（真实项目验证）。

---

## 6. 文件级改动映射

| 文件 | 改动 |
|---|---|
| `frontend/src/stores/workbench.ts` | +`renameAsset`（改名批量替换，P0-B）、+`copyAssetToScene`（P0-A）、名称唯一校验 |
| `frontend/src/core/inheritanceResolver.ts` | 扩展四类解析（prop/style 集合级，场景池优先全局补缺） |
| `frontend/src/core/assetResolver.ts` | （可选）alias 替换时索引重建已自动 |
| `frontend/src/workbench/WorkbenchView.vue` | 场景面板素材库区升级 + Shot 卡片生效资产四类展示 |
| `frontend/src/core/promptMediaTags.ts` | 补全候选来源标记复用（🔄 复制来源） |
| 后端 `director/gen_timeline.py` | **本轮不动**（`@prop/@style` 扩展已砍，见 §5-P1） |
| `frontend/tests/*` | 新增：renameAsset 替换边界 / copyAssetToScene / inheritance 四类 / 素材库 UI 渲染 |

> 后端核心链路（SegmentPlan 资产注入、executor refs）**无需改动** —— V1.5 主要打前端，
> 与用户「先打通数据链、不碰 H3 高级 Prompt」的边界一致。

---

## 7. 验收总纲（每期收敛）

1. **数据闭环**：素材库 → 场景默认 → Shot @引用 → 提交生成 → H3 生成成功，参考图确实是该资产图。
2. **重命名**：改名后全项目 `@` 同步，生成仍带新名资产图。
3. **删除**：删除资产后相关镜头回退继承/无引用，不报错。
4. **可视化**：Shot 卡片四类「生效资产 + 来源徽标」与后端 `report`/refs 实际注入一致。
5. **回归**：V1.4 已验收功能（生成闭环/取消/失败定位/播放）不退化。

---

## 8. 已拍板决策（2026-08-10 用户）

| # | 问题 | 决策 |
|---|---|---|
| 8.1 | 重命名后 `@旧名` 处理 | **改名时批量替换文本**（`@旧名 → @新名`，Prompt 保持纯文本，不引入 ID 绑定） |
| 8.2 | 素材库入口 | **场景面板增强**（两级切换 + 四类网格，不加独立 tab） |
| 8.3 | 继承可视化范围 | **扩展到四类**（人物/地点/道具/风格全部显示「生效 + 来源」） |
| 8.4 | 资产层级模型 | **保持两层池 + 复制快捷**（全局库 + 场景副本，后端零动，先打通数据链） |
| 8.5 | prop/style 集合去重 | **场景池同名优先，全局池只补缺**（按名称判重，UI 不出现重复资产） |
| 8.6 | 改名 token 边界 | `@旧名` 后非资产名字符才替换 + **最长名称优先**（`@林雪·成年`→`@林雪`→`@林`） |
| 8.7 | 后端 `@prop/@style` 扩展 | **本轮不做**（跑通主链后再单开 P1.x） |

---

*规划完稿。下一步：按 §5 P0 → P0 → P1 顺序实施，每期独立验收。*
