# 小说 → AI 导演分镜层 · 架构审计报告

- 审计日期：2026-08-14
- 审计范围：SRC `D:\夸克网盘\ComfyUI_MiniMaxH3_Director`（前端 Vue3+TS / 后端 director Python）
- 审计性质：**只审计不改代码**（用户拍板，先回答 6 问再决定是否动工）
- 目标能力：用户把小说一章自然语言正文直接扔进来 → 系统自动理解剧情、拆 Story Beat、识别场景、生成跨场景交叉剪辑的 Story Timeline、再拆 Shot → 100 章能全部呈现

---

## 0. 你要的东西（需求确认）

你贴的需求我确认理解无误，核心是两条：

1. **输入自由**：直接把小说章节正文扔进去，不需要用户自己加「第一场/第二场/镜头一」。
2. **场景 ≠ 播放顺序**：Scene 是「空间/环境资产容器」，真正决定播放顺序的是 Story Timeline。同一场景可在一章内多次出现，不同场景按剧情需要交叉剪辑（大堂→大堂→后院→大堂→屋顶→大堂）。

这两条，现有系统一条都不满足。下面是证据。

---

## 1. 结论速览（6 问）

| # | 问题 | 结论 |
|---|---|---|
| ① | Scene/Shot/Timeline 数据模型能否承载交叉剪辑 | **不能**。Scene 树结构 = 播放顺序，没有独立 Timeline 层 |
| ② | 哪一层负责剧情拆解 | script_parser.py 规则拆 + script_analyzer.py Qwen 补全；**只认结构化剧本标记，不认自然语言小说正文** |
| ③ | 哪一层负责镜头调度 | **没有独立调度层**。调度被硬编码成「Scene 数组顺序 → 场景内 Shot order」 |
| ④ | 哪一层负责 H3 Prompt | ProductionPlan → DirectorIntent → h3_prompt_builder 三段式；跨镜连续性（#452）建立在「场景内线性 prev」假设上 |
| ⑤ | 哪些必须保持不动 | Asset Registry / Stable Entity Key / Shot 级资产边界 / DirectorIntent 三层可追溯 / h3_prompt_builder / executor / 导出归档 |
| ⑥ | 最小改造方案 | 加一层独立 Story Timeline（播放顺序），拍平+连续性改从 timeline 读；另加自然语言小说导入层 |

---

## 2. 现状证据（读了代码的结论）

### 2.1 数据模型：Scene 树 = 播放顺序

**后端** `director/production_plan.py`：

```
ProductionPlan
├── scenes: List[Scene]          ← 有序，无独立 timeline
│   └── Scene
│       ├── scene_id / title / location_name / time / weather
│       └── shots: List[Shot]    ← 有序，无独立播放索引
└── validation
```

**前端** `frontend/src/models/project.ts`：

```
Project
├── episodes: Episode[]
│   └── Episode
│       ├── scenes: Scene[]      ← 有序
│       │   └── Scene
│       │       ├── location/time/weather/referenceImage/assets/defaultCastId
│       │       └── shots: Shot[] ← Shot.sceneId 唯一归属 + order 排序
│       └── assets（集级全局资产）
└── plan（剧本导入原始 ProductionPlan）
```

关键点：**前端 Scene 其实已经是「场景容器」的雏形**（有 location/referenceImage/assets/默认继承），但它把 `shots` 内嵌在自己下面——这导致「一个场景的镜头必须连续播放」，无法表达「大堂镜头播完 → 切后院 → 再切回大堂」。

### 2.2 时间线拍平：`frontend/src/core/directorCore.ts:47-52`

```ts
const shots: TimelineShot[] = episode.scenes.flatMap((scene) =>
  scene.shots
    .slice()
    .sort((a, b) => a.order - b.order)
    .map((shot) => toTimelineShot(shot, scene, episode, promptOverride)),
);
```

**播放顺序 = `episode.scenes` 数组顺序 × 各场景内 shots 按 `order` 排序。** Scene 就是播放顺序。这个 `flatMap` 产物就是 timeline_data 的 segments，后端 `gen_timeline.py` 按 segments 顺序生成、executor 按顺序执行、导出按顺序合并。全链路共享这一个顺序来源。

### 2.3 跨镜连续性：#452 的「线性 prev」假设

`director/director_intent.py` `build_plan_intents()`（481-549）：

```py
for scene in scenes:
    prev_intent = None          # ← 场景首镜重置
    for i, shot in enumerate(shots):
        intent = build_shot_intent(shot, scene, prev_intent=prev_intent, ...)
        prev_intent = intent    # ← 下镜的「上一镜状态」
```

`camera_template.py` `build_plan_cameras()` 同理：运镜状态机场景内逐镜传递 prev_state、场景切换重置。

**结论：连续性 = 播放顺序的线性相邻。** 一旦交叉剪辑，镜头 3 切回大堂时 prev 被重置为 None（因为从后院场景的视角看它没有「场景内前一镜」），丢失了大堂此前的灯光/布局/角色位置状态。而且没有「跨场景切换」的 transition 语义层（平行剪辑/信息揭示/时间跳跃）。

### 2.4 H3 Prompt 链路（已验收，与场景树无关，天然可保留）

```
ProductionPlan
  → director_intent.build_shot_intent()      → DirectorIntent（USER/RULE/AI provenance）
  → h3_prompt_builder.py                     → 三段式 H3 Prompt
前端：
  Shot.content 五区 → buildShotPromptText()
  → (P0-A #477) POST /prompt/h3 实时重建三段式 → 提交文本
```

`build_plan_intents` 返回 `{scene_id:shot_id → DirectorIntent}`——**每镜独立可寻址**。这个键结构天生不依赖场景树顺序，是交叉剪辑改造的友好起点。

### 2.5 导入链路：`script_parser.py` 只认结构化剧本

`script_parser.py` 的 Scene 切分靠：markdown 标题 / `第X场` / slugline（`内|外|INT.|EXT.`）/ 时间·地点·天气标签行；Shot 拆解靠：`镜头N`标记 / 动作句。**纯小说正文没有这些标记时，会被兜底成「整篇一个场景」+ 按强动作动词硬拆，效果不可用。**

`script_analyzer.py`（Qwen）只补 characters/props/actions/emotion/visual_intent，**不产生剧情单元（Beat）、不产生播放调度**。小说导入需要的那层「理解剧情 → 拆 Beat → 调度场景」完全不存在。

---

## 3. 为什么「场景树 = 播放顺序」挡路

交叉剪辑要「大堂 → 后院 → 大堂」，现有模型只有两条路，都走不通：

- 建两个「大堂」Scene → 场景资产/参考图/继承全部重复维护，违背「一个地点一份资产」原则；
- 把两个「大堂」的镜头塞进同一个 Scene 但调整 order → 它们之间隔着后院的镜头，order 无法表达「间隔播放」。

要表达「播放位置」与「归属场景」解耦，必须有一层**独立于场景树的播放序列**。

---

## 4. 最小改造方案（三层，不推翻已验收链路）

### P0 · 数据模型加 Story Timeline 播放顺序 —— 承载交叉剪辑（纯工程）

**核心思想：Scene 仍是资产容器，新增一个播放顺序数组。**

1. **后端** `production_plan.py`：`ProductionPlan` 加 `timeline: List[str]`（shot_id 播放顺序）。`parse_script` 默认 timeline = 场景树顺序（向后兼容）；新增 `reorder_timeline(plan, order)` 纯函数。
2. **前端** `models/project.ts`：`Project` / `Episode` 加 `timeline?: string[]`。`directorCore.buildTimelineStructure` 从「scenes 树 flatMap」改为「按 timeline 数组收集 shot → 再按 timeline 顺序排序」（无 timeline 字段退化回场景树顺序，老项目不破坏）。**这一处改动就是交叉剪辑的开关。**
3. **跨镜连续性升级（#452 扩展）**：`build_plan_intents` 的 prev 来源从「场景内线性」改为「**timeline 前驱**」（前一个播放的 shot 的 intent，不管它属于哪个场景）；同时 `camera_template.build_plan_cameras` 跨镜状态同样按 timeline 传递。
4. **场景状态补全**：切回大堂时不能丢失大堂状态 → `DirectorIntent.continuity` 扩展为「本镜前驱（timeline 前驱）+ 本场景此前最后状态（scene last state）」双来源合成。加 transition 语义：`_transition_type` 从「线性 9 分支」扩展「场景切换类（平行剪辑/信息揭示/时间跳跃）」。
5. **UI 层**：左侧「场景树」保持（资产编辑），新增「时间线视图」（播放顺序条，可拖拽镜头改变播放顺序）。Scene 资产仍只维护一份。

**影响面**：前端 directorCore + models + WorkbenchView 时间线视图 + 后端 director_intent/camera_template/production_plan。**不碰**：Asset Registry / entity_cleanse / h3_prompt_builder / executor / 导出归档。

### P1 · 自然语言小说导入 —— story_analyzer.py（新功能）

新增一个 Qwen 分析层，**与 script_parser（结构化剧本）并行**，输出仍落到 ProductionPlan（含 timeline）：

```
小说章节正文（自然语言）
  → story_analyzer.py（Qwen3:14B，复用 OllamaBackend + TextSession 显存互斥门）
     ① 剧情理解      → 每镜/每段干了什么
     ② Story Beat   → 剧情节拍单元（进入山雨楼 / 蒙面人观察 / 双方对峙…）
     ③ 场景识别      → 每 Beat 发生在哪（大堂/后院/屋顶），落到 Scene Registry
     ④ Timeline 调度 → 按剧情需要排播放顺序 + transition_reason
     ⑤ 拆 Shot       → 每 Beat 拆成若干 Shot（复用 estimate_duration / 规则兜底）
  → 复用现有 entity_cleanse → asset_matcher → prompt_builder → camera_template → ProductionPlan(含 timeline)
```

用户输入：**直接粘贴小说章节正文**，不需要任何标记。Qwen 失败自动降级为规则兜底 + 警告（沿用现有降级惯例）。

### P2 · 跨章 Global Story Bible —— 100 章连续性的地基

- 以「章/剧情段」为处理单位，**绝不让 Qwen 一次吃 100 章**；
- 维护跨章持久状态：人物 / 人物关系 / 地点 / 地点状态 / 道具 / 世界观 / 已发生剧情 / 当前人物状态；
- 第 50 章出现「山雨楼」时，仍知道它长什么样（复用 Scene Registry + 资产绑定）。

---

## 5. 明确不碰的已验收层（铁律）

以下在本次改造中**保持不动**，它们是交叉剪辑改造的底座：

- **Asset Registry / Stable Entity Key / Alias 归一**（#388/#402/#404）：实体→资产持久化绑定，与场景树无关，天然支持交叉剪辑。
- **Shot 级资产边界**（#471）：per-shot propIds/styleIds，按镜头独立，不依赖场景。
- **DirectorIntent 三层可追溯**（#400）：USER/RULE/AI provenance，按 shot 独立寻址。
- **h3_prompt_builder 三段式 + P0-A 提交链路**（#461/#477-480）：输入是 DirectorIntent，只认 shot，不认场景树。
- **executor / segment_cache / 导出 / 归档 / 播放**：段序由 timeline 给，后端执行不感知「场景」。
- **显存安全门（/api/ps 铁律）、VAE 非动态化修复（#485/#487）**：绝不回退。

---

## 6. 建议实施顺序

| 阶段 | 内容 | 依赖 | 预估改动 |
|---|---|---|---|
| P0 | Story Timeline 播放顺序（交叉剪辑开关） | 无 | 前端 3-4 文件 + 后端 3-4 文件 + 时间线视图 |
| P1 | 自然语言小说导入（story_analyzer.py） | P0（消费 timeline） | 新增后端分析模块 + 导入弹窗扩展 |
| P2 | Global Story Bible 跨章状态 | P1 | 新增跨章状态存储 + 章间传递 |

**建议：先做 P0。** 它不需要任何新 AI 能力，是纯工程改动，做完立刻能：
- 让已经验收通过的《山雨客栈》这类项目支持拖拽镜头实现跨场景交叉剪辑；
- 把 #452 的「线性 prev」升级成「timeline 前驱 + 场景状态合成」的更强连续性；
- 为 P1 的自然语言导入提供现成的播放序列落点。

P0 完成并验收后，再上 P1（这层才是「直接扔小说章节」的核心体验）。

---

## 7. 一句话总结

现有系统是「**结构化剧本 → Scene 树 = 播放顺序 → Shot**」；你要的是「**自然语言小说 → Story Beat → 场景识别 → 独立 Story Timeline（可交叉剪辑）→ Shot**」。两者差的不是镜头生成能力，而是**一层独立的导演调度层（Timeline）+ 一层自然语言剧情理解层（story_analyzer）**。已验收的资产/意图/H3/执行链路全部可以原样复用，不需要推倒重来。
