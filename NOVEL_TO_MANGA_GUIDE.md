# 小说 → AI 漫剧 导入指南

> 适用版本：MiniMax Studio（ComfyUI_MiniMaxH3_Director）· 小说章节导入链路
> 更新时间：2026-08-18

## 一、核心结论：小说文本不需要特定格式

走「📖 小说章节」导入时，系统接收的是**纯自然语言正文（零标记）**——
不需要写「第一场」「镜头一」「内景/外景」等任何标记，直接把小说原文粘贴进去即可。

系统内部流程：`整章原文 → Qwen 整章理解（剧情 Beats + dramatic_function）→ 规则拆 Shot → 审核预览 → 应用为项目`。

---

## 二、两条导入链路对比

| 维度 | 📖 小说章节（story/import） | 📜 剧本（script/import） |
|---|---|---|
| 输入格式 | 纯自然语言，**零标记** | 结构化标记（markdown 标题 / 「第X场」/ slugline / 「镜头 N」） |
| 拆解方式 | Qwen 整章理解 → 剧情 Beats（8–16 个推荐） | 纯规则拆 Scene/Shot + Qwen 语义补全 |
| 适用场景 | 直接拿小说正文开拍 | 已有分场剧本 / 分镜脚本 |
| 界面入口 | 导入弹窗「📖 小说章节」页签 | 导入弹窗「📜 剧本」页签 |

> 同一本小说两种格式都能喂；「小说」链路对格式的要求最低，是默认推荐路径。

---

## 三、小说文本格式建议（3 条）

1. **按「章」导入，不是整本书一把梭**
   - 单章分析上限 `_SOURCE_TEXT_MAX = 12000` 字符（约 6 千汉字内），超出部分截断分析、但原文完整落盘。
   - 推荐单章 **3000–8000 字**：剧情完整、Beats 落在推荐区间（8–16），镜头数适中。

2. **剧情结构完整即可，不用手动分段**
   - Beats 按**真实剧情转折**切分（target 8–16 是推荐区间，非硬性）；章节有起承转合就能拆出高质量镜头。
   - 不够 8 个也不硬凑（剧情结构优先）。

3. **对白与动作都保留**
   - `"引号对白"` → 会被提取为 TTS 台词（走 Voice Cast 配音）。
   - `【】系统提示` → 也会进台词（适合旁白/系统音）。
   - 动作、场景、心理描写 → 进画面提示词（H3）。

---

## 四、操作步骤（前端 SPA）

1. 启动 ComfyUI + 启动 SPA（见文末命令块），浏览器打开 `http://localhost:5173`。
2. 新建项目，或打开已有项目。
3. 点导入入口「**导入（剧本 / 小说章节）**」→ 弹窗内切到「**📖 小说章节**」页签。
4. 粘贴小说正文，或选择 `.md` / `.txt` 文件上传。
5. （可选）选择目标项目 → 开启 **Global Story Bible** 上下文注入（跨章角色/设定保持一致）。
6. 点「分析并导入」→ Qwen 整章理解，生成 **Beats 分组卡片 + 地点列表 + 播放顺序 + 警告**。
7. 审核预览：核对 Beats、地点、播放顺序无误。
8. 点「应用为项目」→ 生成镜头树（每镜带 Prompt 草稿）。
9. 在资产匹配区把角色/地点/道具对到参考图（可先自动扫描共享资产库）。
10. 逐镜审核 Prompt → 逐镜生成（H3）。
11. 每镜 H3 完成后**自动 TTS 台词 + ducking 混音**（Voice Cast 全局面板可调）。
12. 导出成片（逐镜 / 场景 / 全片三级导出）。

---

## 五、小说正文样例（可复制）

> 下面这段是「零标记自然语言」的示例，包含对白、动作、心理、转折，可直接粘贴测试。

```
深秋的雨把山城染成一片灰。林晚站在废弃站台的天桥下，手里攥着一张泛黄的车票，
日期是三十年后。她本该去参加入职面试，可这张车票像有磁力，把她引到了这座早已停运的老站。

“你在找那趟车吗？”一个穿褪色军大衣的老人不知何时站在了她身后。
林晚吓了一跳，车票差点脱手。“您知道这趟车？”
老人眯起眼：“三十年前它每天都来，接走的人，没有一个回来。”
雨声忽然变大。铁轨尽头，一束惨白的灯光刺破雨幕——有车来了。
林晚下意识后退，却被老人一把抓住手腕：“别怕，它等的人，是你。”
车门打开，里面空无一人，只有广播在重复：“终点站，未来。”
林晚回头，却发现来时的天桥已经消失。她咬咬牙，踏进了车厢。
车门在身后合拢的瞬间，她听到老人的声音飘进来：【记住，你不是乘客，你是司机。】
```

---

## 六、启动命令块（Windows 本机）

```cmd
REM —— 终端 1：ComfyUI（确保已启动，默认 8188）——

REM —— 终端 2：启动 MiniMax Studio SPA ——
cd /d D:\夸克网盘\ComfyUI_MiniMaxH3_Director\frontend
npm run dev

REM 浏览器打开：http://localhost:5173
REM （Vite 已配置 /minimax、/api、/ws 等代理到 127.0.0.1:8188，无需额外跨域设置）
```

备选（构建后本地预览，不常用）：
```cmd
cd /d D:\夸克网盘\ComfyUI_MiniMaxH3_Director\frontend
npm run build
npm run preview
```
