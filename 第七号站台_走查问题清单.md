# 《第七号站台》走查问题清单

> 走查《第七号站台》验证 V1.4 + V1.5 过程中发现的问题，统一归类记录。
> 走查完按「UI bug / 资产数据 bug / 提示词问题」分类，优先修走查暴露的问题，不新开功能。
> 关联：`V1.5_VALIDATION_PROJECT_第七号站台.md`（走查规划）、`V1.5_MATERIAL_LIBRARY_PLAN.md`（V1.5 冻结）。

---

## 已发现问题

### Q4 · 项目无自动保存，刷新即丢全部数据（生产事故级）
- **发现日期**：2026-08-10（走查返工）
- **现象**：走查建的《第七号站台》（9 资产图已上传 + 3 场景 + 4 镜头 + 两级资产池复制关系）因**从未手动保存**，刷新 SPA 后项目结构数据全部丢失；磁盘 `minimax_studio/projects/` 只剩一个空「未命名项目」。
- **根因**：项目只存内存（Vue store），**手动点「保存」才写磁盘**；前端无 localStorage 缓存；snapshots 只在**生成视频时**自动落盘（本项目未生成过，无快照可恢复）。资产参考图（`minimax_studio/assets/*.png`）不受影响——上传直接写文件，不依赖 project.json。
- **影响**：编辑大量资产/镜头后一个刷新全丢，对「生产效率」是致命打击。V1.4-P0-2 有 Dirty 状态（💾→●→✓）但用户未注意，P0-3 beforeunload 拦截用户也确认离开了——**提醒机制不够主动**。
- **分类**：数据安全/生产事故（V1.4 稳定生产闭环范畴，走查暴露优先修）
- **处理**：**先修 #99 后回滚 #100**，2026-08-10。用户拍板：①先试自动保存三层防线（防抖 3s + 切后台 + beforeunload）；②实测「退出后数据又没了」（磁盘 project.json 仍是空壳，编辑数据从未写入）→ 用户决定**回滚自动保存，改回手动保存版**（#100）。自动保存未能阻止丢失的可能原因：前端 HMR 未真正生效 / 保存链路静默失败 / 用户未手动保存。**当前状态 = 手动保存**（Dirty 💾→●→✓ + 退出三选一 + beforeunload 拦截原样保留）。
- **待办**：重建《第七号站台》时**关键节点手动点保存**（💾 按钮）；自动保存替代方案（localStorage 缓存 / 编辑即写）留待 V1.6 重新设计后再考虑。

### Q1 · 新建项目无命名入口
- **发现日期**：2026-08-10
- **现象**：点「新建项目」直接进工作台，项目名固定「未命名项目」（home 页可见），集标题固定「第一集」（工作台顶栏可见），均无改名入口。
- **根因**：V1.1.1 只做了打开/新建/导入/保存，从未做项目名/集标题改名 UI。`project_store.py` 新建时硬编码 `title: "第一集"`，前端 `createNewProject` 硬编码 `"未命名项目"`。
- **影响**：不影响走查核心（资产/继承/生成不依赖名称）；影响项目管理和多项目区分。
- **分类**：UI 功能缺口（V1.6 候选）
- **处理**：用户拍板 2026-08-10「先跳过命名继续走查」，记入清单，走查完统一处理。**V1.6-C 已实现（#106，2026-08-10）**：首页项目卡片 ✎ inline 改名 + 工作台顶栏集标题 ✎ inline 改名，纯前端 load→改→save 往返，无新增页面/弹窗。刷新 SPA 即生效。

### Q2 · cast 单选链：多角色同框镜头不支持
- **发现日期**：2026-08-10（走查镜 4）
- **现象**：镜 4 Prompt 含 `@林澈`、人物下拉显式选 林远，但「生效资产」区人物只显示「林远 ⚡镜头指定」，`@林澈` 未进生效区。
- **根因**：`resolveOne`（inheritanceResolver.ts）cast 是**单选链**——显式（林远）命中后直接 return，`@` 匹配分支不执行。规划文档 §5 速查表写的「显式选林远 + Prompt @林澈 即可，生效区会同时列出两条 ⚡镜头指定」是**错误指引**，实现上做不到。
- **影响**：镜 4/5/8/10/11（多角色同框）生效区人物只显示主角色；次要角色靠 Prompt 文本描述但**不会注入 cast 参考图**（后端按 castId 注入）。生成一致性风险。
- **分类**：设计缺陷（V1.5 冻结范围内，不改）
- **处理**：记入清单，走查完归类。V1.6 候选：cast 集合级支持（多 cast 注入）。
- **验证降级**：后续多角色镜头（镜 5/10/11）的走查目标改为「显式选主角色 + Prompt 提次要角色」，只核对主角色徽标。

### Q3 · addShot 预写场景默认值 → 继承徽标误判「⚡镜头指定」
- **发现日期**：2026-08-10（走查镜 4）
- **现象**：镜 4 地点下拉「没动」（用户确认），但「生效资产」区地点显示「地铁站台 ⚡镜头指定」（应为 ↑场景默认）。
- **根因**：`addShot`（workbench.ts:474-475）新建镜头直接 `castId: scene.defaultCastId`、`locationId: scene.defaultLocationId` 写入，**不设 castManual/locationManual**。而 `resolveOne` 显式分支 `defaultId === explicitId`（第 67 行）把「addShot 预填的场景默认」判定为「explicit 镜头指定」。
- **影响**：①新建镜头（默认值已设后）的生效区会**误显示 ⚡**；②**镜 2 的「@命中验证」是假阳性**——镜 2 人物/地点 ⚡ 实际是 addShot 预写默认值被误判为显式，不是 @ 命中，@ 命中分支根本没执行；③污染后续镜 6/7/8 的 @ 验证。
- **分类**：UI/数据 bug（V1.5 走查暴露，优先修）
- **处理**：**已修复（#98）**，2026-08-10。三层修复：① `addShot` 不再预写默认值（保持 undefined，继承链渲染时动态解析）；② 新增 `normalizeShotInheritance` 在 loadProject 时清理「id 有值但 manual 假」残留字段；③ `resolveOne` 去掉 `defaultId === explicitId` 子句。229 测试全绿 + build 通过，双目录已同步。**纯前端改动，刷新 SPA 即生效**；已保存项目需重新打开一次触发 normalize。
- **待办**：刷新 SPA → 重新打开《第七号站台》→ **重新验证镜 2 @命中**（镜 2 人物/地点应显示 ⚡镜头指定，且 @林澈/@地铁站台 chip 正常；若显示 ↑场景默认则说明 @ 未命中，需继续排查）。

### Q5 · SPA 无独立「导出」UI（scene/movie 流式导出无前端入口）
- **发现日期**：2026-08-10（走查「全片导出验证」项）
- **现象**：拟验证全片导出（scene/movie 档 + ffmpeg 直拼、低内存）时，SPA 前端找不到导出按钮。
- **根因**：SPA 前端只有三通道——生成 `exportMode`（segments 走查 / all 成片）+ 单镜下载（segmentMp4）+ 纯前端播放队列。scene/movie 流式导出是**后端 stream_export 能力，前端从未暴露入口**（旧节点 UI 的 Scene Manager/导出中心才有）。`exportMode:all` 成片模式虽然产出整片，但内存受 #103 保护限制（长片可能触发「合并需约 X GB」）。
- **影响**：SPA 内无法低内存导出整片；长片成片只能靠 all 合并（内存敏感）。
- **分类**：UI 功能缺口（V1.6 候选）
- **处理**：走查「全片导出验证」改由**成片模式覆盖**（11 镜 all 合并已跑通并正常播放，2026-08-10 实测）。V1.6 候选：SPA 加「导出」入口走 scene/movie 流式合并，作为长片成片的内存友好替代路径。

### Q6 · 成片模式复验（#105）已通过
- **处理**：2026-08-10 刷新 vite dev 后《第七号站台》成片模式+全部镜头（11 镜）重跑，任务中心正常显示成功、不再弹「生成失败」，成片可打开。#105 误判失败根治闭环确认。

### Q7 · Golden Path 全量验收已通过（V1.6 冻结，2026-08-10）
- **处理**：用户确认 V16_PLAN §5 Golden Path 剩余验收清单全部实现——主线（新建→命名→Scene→资产→生成 8~11 Shot→**修改 Prompt 确认橙色需重新生成**（#111 指纹持久化修复后已验证）→重新生成→全部缓存→导出 Scene→播放→导出整片→下载 MP4→重开项目→再次播放/导出）+ 多角色专项（Shot 05 林雪+陈默→castIds→两个参考图进 timeline→H3 生成→两人身份一致性）全部跑通。**V1.6 正式冻结**，CHANGELOG #112。

---

## 待发现（走查中逐项核对）

- [x] 全局 9 项资产建库/上传参考图是否顺畅（2026-08-10 确认：9 项已建、图已传）
- [x] 场景池复制/新建/留空走全局 是否符合预期（S1 复制4+独有2 / S2 复制3 / S3 复制4 已建）
- [x] 每镜「生效资产」区块徽标与规划一致（镜 1 已验证：人物/地点 ↑场景默认 ×2，道具 🎬×2+🌐×1，风格 🎬×2+🌐×1，全对）
- [x] S1 场景池「林澈」与全局「林澈」同名去重只显示一条（镜 1 验证：人物区只有一条 林澈↑场景默认；全息手表/赛博霓虹夜景同理只显示场景池版本）
- [ ] S3 镜 10「全息手表」全局补缺正常（**待验**）
- [ ] @林澈/@林远/@地铁站台 等引用在 Prompt 框正确高亮 + 进生效区（镜 2 @林澈/@地铁站台 高亮 ✓；**生效区 ⚡ 待 Q3 修复后复验**——#98 已修，刷新+重开项目后确认；镜 3 显式林远 ✓ + @林远 chip ✓）
- [x] 走查模式逐镜生成 8 镜（镜 4-11）全部 ✓（#104，2026-08-10 用户实测：地铁站台 4/5/6 + 站务控制室 7/8 + 天台 9/10/11，队列状态全程正确、未触发失败即停）
- [x] 成片模式 11 镜 all 合并出整片 + 播放正常（#105 复验，2026-08-10 用户实测）
- [ ] 单镜下载/播放队列/取消重生成（**专项未验**——#83/#84 已修，逻辑有单测覆盖；实际手验可并 V1.6 补验）
- [ ] 重开项目资产保留（**待验**——需手动保存后重开确认）
- [ ] 3 场景顺序生成、场景内 r2v 续接、林澈跨场景长相一致（**成片已出，抽查未做**——镜 4-11 成片可从头到尾看一遍核对）
