# MiniMaxH3Director 导演台 · 操作使用指南（给 AI 助手 / 新用户的说明书）

> 本文件是**操作说明书**：告诉你这个"AI 导演台"怎么用、每个按钮/开关/字段是干什么的、完整流程怎么走、出问题怎么排查。
> 想了解内部改了什么、代码坑在哪 → 看 [CHANGELOG.md](CHANGELOG.md)；想了解架构设计 → 看 [STATE_MANAGER_DESIGN.md](STATE_MANAGER_DESIGN.md)。
> 本文件所有字段名与实际代码一致，可放心照着操作。

---

## 1. 这是什么东西

**MiniMaxH3Director** 是一个 ComfyUI 自定义节点，用 **MiniMax H3** 模型做 AI 短剧/连续镜头视频生成。它解决的核心问题是：**多个镜头（segment）生成时，人物、场景、状态能不能连续**——不像单段生成那样每段各管各的。

它把"拍连续剧"的流程搬进节点：

```
剧本（分镜表） → 导演核心（状态库 + 镜头路由） → 每段用 MiniMax H3 生成视频 → AI 质检（可选）
```

所谓"导演"，就是它在生成每个镜头时自动处理：上一镜头尾帧怎么衔接、角色/场景参考图怎么注入、叙事状态怎么往下传、严重错误要不要重跑。

**两个关键模型区分（很重要）**：
- **r2v**（参考主体生视频）：用 `ref2va` 模型，参考**图片**为主，适合绝大多数"状态驱动"镜头（对话/走路/环境/情绪）。
- **fl2v**（首尾帧生视频）：用 `fl2va` 模型，锁定**首帧/尾帧**像素，适合"状态变化镜头"（开门/拔剑/变身/打斗/跳跃/落地）。

---

## 2. 核心概念（先理解这些）

| 概念 | 说明 |
|---|---|
| **segment（段/提示词组/镜头）** | 一个镜头。r2v 模式下是一组"提示词 + 参考素材"，fl2v 模式下是一组"提示词 + 首帧/尾帧"。 |
| **Scene（场景）** | 导演系统一级对象：一组素材（角色/场景/道具/风格参考）+ 归属它的镜头（Shot）。镜头通过「所属场景」下拉挂到场景；导出按场景编码成 `SceneNN.mp4`，全片再用 ffmpeg 拼 `Movie.mp4`。 |
| **全局资产库（assets）** | 角色库 + 场景库 + 道具库 + 风格参考库。每段可选一个角色 + 一个场景，生成时自动作为参考图注入，锁一致性。场景素材组优先级高于全局资产库。 |
| **自动续接上段** | 本段不传素材时，自动用上一段的尾帧 + 继承的角色/场景图做锚点，保证跨镜连续。 |
| **状态跟踪** | 把"故事走到哪了"（地点/时间/动作/情绪）用文本前缀传给下一镜。 |
| **智能尾帧** | 续接锚点默认用上一段真末帧；若末帧较模糊/过曝，自动改选更清晰的靠后帧。 |
| **衔接模式（continuityMode）** | 每段决定走 r2v 还是 fl2v：auto=按提示词关键词自动判断，ref2va/fl2va=手动强制。 |
| **Qwen3-VL 三级反馈** | 可选的 AI 质检：一级提取状态、二级查一致性、三级查严重错误并重跑。 |

---

## 3. 界面布局

### 3.1 r2v 批（批量生成）工具栏

在「参考主体生视频」工作流里，批工具栏上有这些开关：

| 开关 | 字段 | 默认 | 作用 |
|---|---|---|---|
| **+ 添加提示词组** | — | — | 新增一个镜头。 |
| **自动续接上段** | `r2vAutoContinuity` | 开 | 本段不传素材时，自动用上一段尾帧 + 继承角色/场景图续接。 |
| **全局资产库** | `globalAssetsEnabled` | 开 | 显示资产库面板（角色库/场景库/道具库/风格参考库）。 |
| **状态跟踪** | `stateTrackingEnabled` | 开 | 跨镜传递叙事状态（文本前缀）。 |
| **智能尾帧** | `smartTailEnabled` | 开 | 续接锚点智能选帧（末帧模糊时换更清晰的帧）。 |

> 注：这些开关只出现在 r2v 批。fl2v 面板有自己的开关（见 3.3）。开关字段缺失时**后端默认视为开启**（显式 false 才关），所以工作流旧文件重启后不需要重新勾。

### 3.2 每个 r2v 镜头卡片

每个镜头卡片包含：

| 控件 | 字段 | 说明 |
|---|---|---|
| **提示词** | `seg.prompt` | 本镜头画面描述。支持 `@角色名` 自动匹配资产、`<Picture N>`/`<Video N>`/`<Audio N>` 引用素材。高亮层会把它渲染成带缩略图的卡片（底层仍是纯文本）。 |
| **负面提示词** | `seg.negativePrompt` | 输入后会清空（当前版本不保留）。 |
| **参考图槽位** | `seg.refs` | 本镜头的参考图（角色/场景/尾帧等）。 |
| **衔接模式** | `seg.continuityMode` | auto / ref2va / fl2va。auto=按提示词关键词（拔剑/变身/打斗/跳跃/开门/奔跑…）自动转 fl2v。 |
| **关键镜头** | `seg.consistencyCheck` | 跟随全局 / 开启 / 跳过。控制本段是否跑 Qwen 二级一致性检测（对比生成帧 vs 资产图）。 |
| **智能尾帧** | `seg.smartTail` | 跟随全局 / 开启 / 关闭。段级覆盖总开关。 |
| **状态变更** | `seg.stateChange` | 本镜结束后要传给下一镜的状态描述（如"陆玄拔剑冲向敌人"）。用户手填优先，Qwen 提取兜底。 |
| **角色 / 场景** | `seg.castId` / `seg.locationId` | 下拉选择本段用的全局资产。不手动选时按提示词里的资产名自动匹配。 |
| **所属场景** | `seg.sceneId` | 下拉把本镜头归属到某个场景（Scene Manager 里建的）。用于按场景导出。 |

### 3.6 顶部导航：场景管理 / 素材库 / 导出中心

时间线面板顶部有 4 个 tab：**时间线 · 场景管理 · 素材库 · 导出中心**。

**场景管理**（`timeline.scenes` 块）：

| 控件 | 字段 | 说明 |
|---|---|---|
| **+ 新建场景** | — | 新建一个场景，自动命名 `Scene NN`，可在详情里改名称/地点/时间。 |
| **场景卡片** | `sc.name` / `sc.location` / `sc.time` | 点卡片看详情；卡片上可删除、上移/下移（按 `order` 排序）。 |
| **场景素材组** | `sc.assets.{cast,locations,props,styles}` | 每个场景独立素材组，**优先级高于全局资产库**。角色/场景可设"默认"（本场景段自动带入）。 |
| **本场景镜头** | — | 列出归属本场景的镜头（Shot 序号 + 时长 + 提示词摘要）。 |
| **导出此场景** | — | 只跑本场景镜头 → `{前缀}_{计数器}_SceneNN.mp4`（自动把导出方式设为「场景导出」并触发运行）。 |

**素材库**：只读总览——全局资产库四类目（角色/场景/道具/风格参考）缩略图 + 各场景素材组数量。点「编辑」切回时间线。

**导出中心**：
- 每场景一行（镜头数 + 时长 + **导出此场景**按钮，无镜头时按钮禁用）。
- **全片导出**按钮：把所有场景 mp4 用 ffmpeg 直拼成 `Movie.mp4`（几乎零内存）。

> 在场景管理里删场景会自动清空该场景下镜头的「所属场景」（镜头不删）。

### 3.3 fl2v 面板

fl2v 面板（首尾帧生视频工作流）的开关：

| 开关 | 字段 | 说明 |
|---|---|---|
| **自动续首帧** | `continuityEnabled` | 第 2 镜起首帧自动用上一镜尾帧。 |
| **续接方式** | `handoffMode` | `image`=参考图续接（默认，用尾帧当参考图）；`video`=从视频续接（用上一段整段视频当参考，更吃显存）。 |
| **重叠帧** | `continuityOverlapFrames` | 首尾帧重叠帧数。 |
| **首帧 / 结束帧** | `refs[0]` / `refs[1]` | fl2v 段的手动首帧/尾帧槽位（手动上传优先）。 |

### 3.4 AI 导演设置面板

r2v 批工具栏下方有一个「AI 导演设置」折叠面板，独立于 R2V 工具栏（Qwen 属于整个导演系统）：

| 开关 | 字段 | 说明 |
|---|---|---|
| **启用（AI 导演判断）** | `qwenVlEnabled` | 总开关，默认关（字段缺失=关）。 |
| **反馈级别** | `qwenVlLevel` | `1`=状态提取；`2`=+一致性检测；`3`=+失败重跑。 |

级别说明：
- **1 级**：每段生成后用 Qwen 看尾帧，提取 角色/地点/时间 + 角色状态(姿态/朝向/情绪) + 镜头状态(景别/机位)，自动更新状态跟踪。
- **2 级**：在一级基础上，对有资产注入的"关键镜头"跑一致性检测（生成帧 vs 角色/场景资产图），报告 `一致性: 角色=…; 场景=…; 服装=…`。
- **3 级**：在二级基础上，Qwen 报严重问题（多手/崩坏/换脸）+ 规则层像素检测（过暗/过曝/纯色/模糊）**双确认**都成立时，自动换种子重跑一次。

> 开启后每次生成结束，报告会显示 `反馈: Qwen3-VL 状态=「…」`、`一致性: …`、`重跑: …` 等行。

### 3.5 导出方式（Movie → Scene → Shot 三级）

「导出方式」下拉在输出面板，4 个档位（顺序 = 推荐度）：

| 档位 | 字段值 | 输出 | 说明 |
|---|---|---|---|
| **场景导出（默认/推荐）** | `scene` | 每场景一个 `{前缀}_NNNNN_Scene01.mp4`… | 按地点连续段自动归成场景；场景内做曝光/颜色统一 + 接缝优化再编码；内存峰值 ≈ 单场景。 |
| **全片导出** | `movie` | 一个 `{前缀}_NNNNN_Movie.mp4` | 先按场景处理落盘，再用 ffmpeg 直拼所有场景 mp4 → Movie.mp4，**几乎不吃合并内存**。需要 ffmpeg（ComfyUI 环境一般自带 imageio-ffmpeg）。 |
| **分镜导出** | `segments` | 每镜一个 mp4 | 每段时间轴片段单独输出，不做场景合并。 |
| **全片导出（内存合并）** | `all` | 一条合并视频 | 旧方式，把所有段的 float32 帧在 CPU 内存里 `torch.cat`，**仅适合短片**（总帧数少）；9 段 × 20s 会爆内存（见 FAQ）。 |

场景边界 = **同地点（location）连续拍摄的镜头**归为同一场景；跨地点即换场。老工作流加载后默认值回退为 `scene`。

---

## 4. 完整操作流程（以 r2v 连续剧为例）

### 第 1 步：建资产库
1. 打开「全局资产库」开关，展开角色库/场景库/道具库/风格参考库。
2. 上传角色图（文件名 = 角色名，如 `陆玄.jpg`）、场景图（如 `圣城广场.jpg`）。
3. 每个库可设一个"全局默认"（新建段自动回填）。

### 第 2 步：写分镜
1. 点「+ 添加提示词组」，逐段写提示词。
2. 提示词里**提到角色/场景名**（如"陆玄站在圣城广场…"）→ 自动匹配资产并注入参考图。
3. 需要动作推进的镜头，提示词写**具体动作动词**（"低头 + 伸手轻抚碑文 + 镜头拉近特写"），不要只写"缓缓低头"这类弱动作——模型对参考图遵循很强，提示词弱会复刻上一镜。
4. 第二镜起需要接续的，勾上「自动续接上段」（默认开），不用手动传尾帧。
5. 需要特殊连续动作的镜头，把「衔接模式」设成 `fl2va` 并上传首帧/结束帧；或留 `auto` 让它按关键词自动路由。

### 第 3 步：建场景 & 把镜头归属到场景（导演系统一级对象）
1. 切到顶部「**场景管理**」tab → 点「+ 新建场景」→ 在详情里改名称/地点/时间。
2. 切回「时间线」，每张镜头卡片的「**所属场景**」下拉选它属于哪个场景；也可给场景单独上传素材组（优先级高于全局资产库）。
3. 到「素材库」tab 可总览全局资产和场景素材组；到「导出中心」tab 可对每个场景一键导出。

### 第 4 步：开状态跟踪 + AI 导演
1. 「状态跟踪」开（默认开）。每镜的「状态变更」填本镜结束后的状态（也可不填，让 Qwen 1 级自动提取）。
2. 「AI 导演设置」→ 启用 Qwen。首次先用 1 级，跑通后升 2/3 级。

### 第 5 步：生成 + 看报告
1. 点运行。每段生成完报告会显示：`衔接:`（续接方式）、`状态跟踪:`（前缀/变更）、`反馈: Qwen3-VL 状态=…`、`一致性: …`、`重跑: …`。
2. 关键行解读：
   - `衔接: first_frame=图片锚点续接(ref_image_0=上段尾帧 + 全局资产N张)` → 跨镜连续机制生效。
   - `衔接: first_frame=上一段尾帧(…帧→取末帧)` → fl2v 硬锁生效。
   - `反馈: Qwen3-VL 状态=「…」` → 1 级生效。
   - `一致性: 角色=True; 场景=True; 服装=True (…一致)` → 2 级生效，画面忠于设定。
   - `重跑: 已重跑(原因; seed=…→…)` → 3 级触发重跑；`不重跑: …` → Qwen 报了但规则不确认，按设计不重跑。

### 第 6 步：导出
1. 输出面板「导出方式」选档位：默认 `scene`（场景导出，推荐）；想要整片选 `movie`；只想要单个镜头成品选 `segments`；短片想要一条完整视频选 `all`。
2. 也可以到「**导出中心**」tab：点某场景的「导出此场景」自动切到 scene 档并只跑该场景镜头；点「全片导出」跑 movie 档（ffmpeg 直拼各场景 mp4）。
3. 报告会显示 `Scene groups: N scene(s) (x/y/z shot(s) each)` 和每场景一行 `Scene N: merged M shot(s), K frames → xxx_SceneNN.mp4`。
4. scene 档：每个场景一段视频，直接落在 ComfyUI 输出目录；movie 档：额外多一个 `xxx_Movie.mp4`（ffmpeg 直拼各场景，几乎不占内存）。

---

## 5. 常见问题排查（速查）

| 现象 | 原因 / 排查 |
|---|---|
| 报告显示 `Text-to AV (no keyframes or references)` | r2v 段无参考媒体且没开启自动续接 → 退化成文生视频。先确认「自动续接上段」已勾（旧工作流重启后字段可能丢失，需要重新勾）。 |
| 段 2 画面复刻段 1 / 动作没推进 | 提示词写弱了。参考图锁一致性、动作推进靠提示词——写具体动作动词 + 新机位/景别。 |
| 人物/场景跨镜不一致 | ①资产面板是否传图 + 每段下拉选对 ②「全局资产库」开关 ③提示词是否写足具体动作。资产图管一致性、提示词管动作。 |
| FL2VA 段首帧不是上段尾帧 | 手动上传了首帧会优先（手动优先于自动续接）；检查「衔接模式」是否真的 = fl2va，以及是否有上一段生成结果。 |
| Qwen 反馈没出现 | ①「AI 导演设置」启用开关是否勾（默认关，重启后要重勾）②transformers 依赖/模型是否就位（见 CHANGELOG #21）③报告是否显示"反馈不可用"。 |
| 画面明显崩了（多手/换脸）但没重跑 | **按设计不重跑**——3 级是 Qwen + 像素规则双确认，多手是"画得清楚但画错"，像素层查不出来，不触发。要触发重跑需画面同时有过曝/模糊等像素异常。 |
| 提示词框滚动时文本超出边界 | 高亮层 bug（已修复）。重启 Comfy Desktop 生效；若再出现，检查 `.bd-phl-hl` 高度是否被锁死。 |
| 某镜没显示"一致性" | ①面板级别 ≥2 ②该段是否有资产注入 ③该段「关键镜头」是否被设为跳过。 |
| 秒数/数字输入框值自己跳 | 已知已修复（防抖覆盖半截输入）。重启生效。 |
| 「全部导出（内存合并）」报内存不足 `DefaultCPUAllocator: not enough memory` | CPU 内存合并整段视频放不下。**先查每段时长/总帧数**——9 段 × 20s ≈ 45GB 必然爆。解决办法（任选）：①导出方式改「场景导出」或「全片导出」（场景内合并/ffmpeg 直拼，不吃合并内存）②每段缩短到 5–10s（勿超 15s）③降低画布分辨率 ④「选择运行」分批生成。新版已加预检：放不下会在生成前直接报中文提示，不再裸崩。 |
| 选了「场景导出」但某场景没出来 / 报 `Scene N produced no frames` | 该场景的镜头没在「选择运行」里勾选，且无缓存可填。把它加进运行选择，或改「分镜导出」跳过它。 |
| 「全片导出」报 ffmpeg 找不到 | 需要 ffmpeg 合并场景 mp4。ComfyUI 环境一般自带 imageio-ffmpeg；若没有，装 `pip install imageio-ffmpeg` 或把 FFmpeg 加进 PATH，或改「场景导出」。 |
| 想导出的"一个地点的一段剧情"自动合并了 | 场景边界 = 同 location 连续段自动归一组——这正是「场景导出」的设计。不同地点之间会拆成不同场景。 |
| 场景导出没按我想要的场景拆 / 段没进对场景 | 每段的「所属场景」下拉（`seg.sceneId`）显式指定优先；没指定时后端按 location 兜底。去「场景管理」看每场景镜头列表，把镜头归属调对。 |
| 删了场景，镜头还在 | 按设计：删场景只清空该场景下镜头的「所属场景」（`sceneId` 置空），镜头本身不删。 |
| 场景顺序不对 | 场景排序看 `order` 字段（场景卡片上的 ↑/↓ 调整），不看数组下标。 |

---

## 6. 一次完整实测示例（可照着跑）

**设定**：角色 = 陆玄，场景 = 圣城广场。

**资产**：`陆玄.jpg`（全身设定图）、`圣城广场.jpg`（广场全景）。

**分镜（3 段）**：
1. 段1：`陆玄站在圣城广场中央，身姿挺拔，仰头望向远处高大的天碑，阳光洒落，衣袍随风微动。镜头缓缓推近。`
2. 段2：`陆玄目光一凝，猛地拔剑冲向对面敌人，剑光闪动，脚步迅疾踏出，衣袍翻飞。`（含"拔剑"→ auto 路由自动转 fl2v 硬锁）
3. 段3：`陆玄挥剑斩下，剑锋划破空气，动作干净利落，收势站定。`

**开关**：自动续接上段 = 开；全局资产库 = 开（段1 手动选 陆玄/圣城广场）；状态跟踪 = 开；AI 导演 = 启用，级别 2 或 3。

**预期报告**：段1 `图片锚点续接` + `一致性: 角色=True; 场景=True`；段2 `first_frame=上一段尾帧` + 状态前缀沿用段1 状态；段3 同段2。

---

## 7. 文件/目录速查（给会改代码的助手）

| 路径 | 干什么 |
|---|---|
| `director/plan.py` | 数据模型（所有字段定义） |
| `director/gen_timeline.py` | timeline JSON → plan 解析（命名匹配/关键词路由/三态解析） |
| `director/executor_core.py` | 核心执行：生成、续接、资产注入、状态跟踪、Qwen 三级、报告 |
| `director/state_rule_check.py` | 3 级规则检测（纯像素） |
| `director/qwen_vl_feedback.py` | Qwen3-VL 反馈（状态提取/一致性/问题检测） |
| `director/vlm_backends.py` | VLM 后端（llama.cpp / transformers） |
| `director/fl2v_timeline.py` | fl2v 面板 plan 构建 |
| `director/stream_export.py` | 场景/流式导出：SceneNN.mp4 编码、ffmpeg 合并、音频拼接、解码回张量 |
| `director/segment_cache.py` / `vram_cleanup.py` | 段缓存 / 显存清理 |
| `web/js/minimax_image_batch.js` | r2v 批 UI（工具栏/段卡片/资产面板/高亮） |
| `web/js/minimax_fl2v.js` | fl2v 面板 UI |
| `web/js/minimax_director_panel.js` | AI 导演设置面板 |
| `web/js/minimax_prompt_mentions.js` | 提示词素材高亮层 |
| `web/js/minimax_scene_manager.js` | Scene Manager 面板（场景 CRUD/素材组/每场景导出） |
| `web/js/minimax_export_center.js` | 素材库总览 + 导出中心（每场景导出/全片导出） |
| `web/js/minimax_timeline.js` | timeline 构建/加载/持久化（含 payload 白名单） |
| `web/js/minimax_i18n.js` | 中英文案 |
| `SCENE_PROMPT_GUIDE.md` | **面向 AI 的分镜数据规范**：Scene→Shot 结构、提示词放置规则、场景归属/排序、资产优先级、完整示例 JSON、生成/解析两套流程、常见坑。发给别的 AI 用它自动读取分镜结构。 |
| `AI_PROMPT_WRITER_INSTRUCTIONS.md` | **给外部 AI 的写作指令**：只输出纯文本分镜提示词（每镜一段 + 场景分组），禁止输出 JSON/工作流文件。发给"帮你写提示词的 AI"用这份，别发 SCENE_PROMPT_GUIDE.md（那是数据结构规范，会诱导它生成 JSON）。 |

**修改铁律**：改任何东西必须**源目录 + 部署目录双同步**（源 `D:\夸克网盘\ComfyUI_MiniMaxH3_Director` = 部署 `D:\Comfy-Desktop\ComfyUI (1)\ComfyUI\custom_nodes\ComfyUI_MiniMaxH3_Director`）。给 r2v 段加新 per-segment 字段必须同步进 `buildTimelinePayload` 白名单。
