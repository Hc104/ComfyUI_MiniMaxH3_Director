# P4 实机验收：《吐槽成真》4 镜（v2.0 拍板）

> 2026-08-18 · CAMERA_RULES_V2 §5.2/§7 P4 行 · 已确认 A/B 路径 = **零代码改动**（当前 build 直接生成 v2.0）
> 剧本：`吐槽成真_4镜剧本.md`（SRC 与 DEP 均已同步，md5 一致）

## 0. 前置（务必先做）

**重启 Comfy Desktop** 加载最新后端（P1 四模块接线 + P3 `/style/presets` 路由均为 2026-08-18 新增代码，未重启不生效）。重启后执行预检：

```powershell
# ① 后端路由预检（期望返回 7 个 presets；404 = 后端未加载新代码，先重启）
curl.exe -s http://127.0.0.1:8188/minimax/director/style/presets

# ② 剧本导入路由预检（期望返回 {"error": "empty_script", ...} 400 = 路由存在）
curl.exe -s -X POST http://127.0.0.1:8188/minimax/director/script/import -H "Content-Type: application/json" -d "{}"

# ③ 双目录同步核验（期望 md5 一致）
certutil -hashfile "D:\夸克网盘\ComfyUI_MiniMaxH3_Director\吐槽成真_4镜剧本.md" MD5
certutil -hashfile "D:\Comfy-Desktop\ComfyUI (1)\ComfyUI\custom_nodes\ComfyUI_MiniMaxH3_Director\吐槽成真_4镜剧本.md" MD5
```

**前端版本确认**：SPA 顶栏右侧 Build ID 应显示 `v1.x·b260818-xxxx`（2026-08-18 构建）。若 SPA 用的是部署版 dist，需重新 build 后替换；若用 vite dev server（5173）则热重载即可。确认后左侧面板出现 **Visual Style** 预设下拉（7 预设，默认「抖音半写实漫剧」）即代表 P3 前端已生效。

## 1. 剧本内容（SPA 导入时粘贴或上传 `吐槽成真_4镜剧本.md`）

```markdown
# 《吐槽成真》4 镜 A/B 实机剧本

## 第一场：豪华私人办公室

地点：豪华私人办公室
时间：午后
天气：晴

镜头 1：顾琰宸从西装内袋掏出支票，将支票从手中甩落到林薇薇面前的桌面，支票落桌后轻微滑动。林薇薇没有伸手去拿。

镜头 2：林薇薇伸出两根手指，漫不经心地捏起桌上的支票，缓缓抬到眼前，嘴角浮现一抹带有嘲讽意味的笑。

镜头 3：林薇薇用支票轻轻敲击桌面，目光直视顾琰宸。
林薇薇说："就这？"

镜头 4：顾琰宸原本冷漠的表情突然凝固，瞳孔微微收缩，眉头极轻地皱起，第一次露出明显意外。
```

> 规则拆镜预期：1 场景 / 4 镜 / shot_03 对白 speaker=林薇薇（已验证）。

## 2. SPA 操作步骤

1. **新建项目**：项目选择页 → 新建，命名《吐槽成真》。
2. **导入剧本**：导入入口选剧本文件（或粘贴上述文本）→ 走 Qwen 语义补全 → 审核预览。
3. **审核**（每镜核对，重点看 DirectorIntent 派生字段）：
   - 角色：顾琰宸（顾氏集团总裁）、林薇薇（职场女性）；地点：豪华私人办公室。
   - 每镜 `core_action`（一镜一核心动作）、`expression`（心理可视化）、`tension`/`rhythm`（节奏）、`camera`（运镜）应与 §5.2 目标一致。
   - 资产绑定：顾琰宸/林薇薇 各绑一张角色参考图（可先上传资产图再绑定，或从资产库复制）。
4. **逐镜生成**：确认 4 镜状态灯就绪 → 生成（fl2v 首镜 / r2v 或 fl2v 续接均可，按 SPA 建议）→ 等待解码完成。
5. **回看对比**：每镜播放成片，按下方维度表核对。

## 3. A/B 对比维度（v2.0 目标 = CAMERA_RULES_V2 §5.2）

> ⚠️ 现状说明：SPA 项目存储与部署目录均已无 v1.0 时代成片 mp4（山雨客栈 12 镜不在库）。A/B 对比以 §5.2 目标输出为准；如你手头有 v1.0 成片备份，可一并打开对照。

| 镜 | v2.0 目标（tension·rhythm·role） | 核心动作（一镜一动作） | 镜头/运镜 | 验收要点（对画面满意？） |
|---|---|---|---|---|
| 1 | tension 1 · hold · info（施压） | 顾琰宸甩支票落桌轻微滑动 + 林薇薇**不伸手** | 中近景，平视，静止 | 是否「一个动作+不接支票的细节」，没有多余动作堆砌 |
| 2 | tension 2 · build · emotion（反击建立） | 林薇薇两指捏支票，缓缓抬到眼前 | 中近景，**缓慢推近** | 嘲讽笑是否到位、推镜是否克制 |
| 3 | tension 3 · peak · emotion（反转峰值） | 林薇薇用支票敲桌面直视顾琰宸 + 对白「就这？」 | **特写推近面部后定格** | 峰值定格而非无脑推镜；「就这？」是否清晰 |
| 4 | tension 3 · reaction · 震惊（**拍顾琰宸**） | 顾琰宸表情凝固、瞳孔微缩、眉头极轻皱起 | **锁定特写，静止** | 是否拍到「听众懵了」而非林薇薇；静止定格力量 |

**整体画风**：抖音半写实漫剧（半写实动漫人物 + 电影级自然光 + 85mm 浅景深 + 低饱和暖调）是否稳定、人物是否一致、无字幕是否干净。

## 4. 判定

- 全部 4 镜画面满意 → **v2.0 上线**（P4 完成，通知我收尾：更新记忆/CAMERA_RULES_V2 状态/CHANGELOG）。
- 局部不满意 → 反馈具体镜号 + 问题（如「镜3 推镜太快」「镜4 没拍到错愕」），我来判断是规则层参数还是剧本措辞问题。
- 整体不如 v1.0 → 按 CAMERA_RULES_V2 §9 回滚策略处理。
